# CI/CD with Azure Static Web Apps

Small academic project used to practice a CI/CD workflow with GitHub Actions and Azure Static Web Apps.

## What it demonstrates

- Automatic deployment on pushes to `main`
- Preview deployments for pull requests
- Cleanup when a pull request is closed

The application itself is a minimal HTML, CSS, and JavaScript page; the focus of the repository is the deployment workflow.
